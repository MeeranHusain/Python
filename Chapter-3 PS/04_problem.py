# Replace the double space from problem 3 with single spaces.

name = "Meeran is a  good  boy"
# name = "Harry is a good  boy and  "

print(name.replace("  ", " "))
print(name)     # Strings are immuttable which means that you cannot change them by running functions on them