a ='Meeran'       # Single quoted string 
b = "Meeran"      # Double quoted string 
c = '''Meeran'''  # Triple quoted string

# String is immutable, because once string is created it can't be change that string, if you want to change the particular character of a string you cannot change the string. you have to create another string.

# ------------------------ String Slicing ------------------------
name = "Meeran"
nameshort = name[0:3]       # start from index 0 all the way till 3 (excluding 3)
print(nameshort)
character1 = name[3]        # get the one character at index of a string.
print(character1)

# print(len(name))          # len of string

